---
page: AdminPage
source: src/pages/AdminPage.tsx
---

> 狀態：初始為 [ ]、完成為 [x]  
> 測試類型：前端元素、導航與 function 邏輯  
> **本次驗證**（2026-04-01）：`vitest run --run` → `AdminPage.test.tsx` **5/5 通過**；專案合計 **24/24 通過**

---

## [x] 【前端元素】應顯示標題「🛠️ 管理後台」、「← 返回」連結、區塊標題「管理員專屬頁面」及三項說明文字

**範例輸入**：`localStorage` 具備 token，MSW `/api/me` 成功，進入 `/admin`  
**期待輸出**：可見 `h1`「🛠️ 管理後台」、`link`「← 返回」、`h2`「管理員專屬頁面」；文案含「只有 admin 角色可以訪問」「user 角色會被重定向」「受路由守衛保護」

---

## [x] 【前端元素】當使用者角色為 admin 時頂部角色徽標應顯示「管理員」

**範例輸入**：`msw_user_role` 為 `admin`（或預設），已登入並進入管理頁  
**期待輸出**：`header` 內 `.role-badge` 顯示「管理員」

---

## [x] 【前端元素】當使用者角色為 user 時頂部角色徽標應顯示「一般用戶」

**範例輸入**：`localStorage` 設定 `msw_user_role` 為 `user`  
**期待輸出**：頂部角色徽標顯示「一般用戶」

---

## [x] 【導航與 function 邏輯】點擊「登出」應清除登入狀態並導向登入路由

**範例輸入**：已登入並在 `/admin`，點擊「登出」  
**期待輸出**：路由顯示登入路由替身內容；`localStorage` 無 token

---

## [x] 【導航與 function 邏輯】點擊「← 返回」應導向儀表板路由

**範例輸入**：在 `/admin` 點擊「← 返回」  
**期待輸出**：畫面為儀表板路由替身內容

---
